# Copy to .env for local development, then replace the placeholder password.
# The app does not load .env automatically; export it before running uvicorn.
DATABASE_URL=postgresql+psycopg://rpq_user:<password>@127.0.0.1:5432/rpq_db
